<div class="container">
{{#if questionSend}}

  {{#if cards}}
    <ul class="cards">
      {{#each cards}}
        <li>
          <h2>{{name}}</h2>
          {{#if url.original}}
            <img src="{{url.original}}">
          {{/if}}
        </li>
      {{/each}}
    </ul>
  {{else}}
    Sem cards
  {{/if}}

{{else}}

  <div class="row">
  <div class="col col-md-6 col-md-offset-3">
    <h1 class="stylist-question-title">O que você deseja?</h1>

    <form action="/stylist" method="post">
      <div class="form-group">
        <input class="form-control input-lg" name="question" placeholder="Nos conte :)" value="{{questionSend}}" />
      </div>
      <div class="pull-right">
        <button class="btn btn-success btn-lg">Pesquisar</button>
      </div>
    </form>

  </div>
{{/if}}
</div>