<div class="container">
{{#if questionSend}}
  <ul class="cards">
    {{#each data.cards}}
      <li>
        {{url.original}}
      </li>
    {{/each}}
  </ul>
{{else}}

  <div class="row">
  <div class="col col-md-4 col-md-offset-4">
    <h2 class="stylist-question-title">O que você deseja?</h2>

    <form action="/stylist" method="post">
      <div class="form-group">
        <input class="form-control" name="question" value="{{questionSend}}" />
      </div>
      <button class="btn btn-primary">Pesquisar</button>
    </form>

  </div>
{{/if}}
</div>